// le folder (auth) permet de regrouper les pages qui sont liées à l'authentification de l'utilisateur
// Organiser les fichiers de votre projet sans affecter la structure de l'URL
// donc pour accéder à cette page il faut diriger vers /forgot-password à l'URL de base et non pas /auth/forgot-password

export default function ForgotPassword() {
  return <h1>Forgot password</h1>;
}
