// _lib, _ signifie que ce fichier est un fichier privé et ne doit pas être accessible directement dans le navigateur

export default function PrivateRoute() {
  return <h1>You cannot view this in the browser</h1>;
}
